{
  "name": "scary-teacher",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "@truffle/hdwallet-provider": "^2.0.3"
  },
  "devDependencies": {
    "truffle-plugin-verify": "^0.5.24"
  },
  "scripts": {
    "compile": "truffle compile",
    "deploy local": "truffle migrate",
    "deploy local only specified": "truffle migrate --f 3 --to 3",
    "deploy rinkeby": "truffle migrate --network rinkeby",
    "verify": "truffle run verify ScaryToken --network rinkeby",
    "test": "truffle test"
  },
  "author": "DICOTA",
  "license": "ISC"
}
